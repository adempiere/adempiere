@Override
protected String doIt() throws Exception
{
    /*
    List<Integer> Ids = (List<Integer>) getSelectionKeys()
        .stream()
        .filter(mrpId -> mrpId > 0)
        .map(mrpId -> {
            // ... todo el código del mapeo ...
            return mrpId;
        })
        .distinct()
        .collect(Collectors.toList());
    */
    
    return ""; 
} 